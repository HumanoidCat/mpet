# Fixtures de audio para tests de `src/ai/`

Audios pregrabados para probar el pipeline de IA **sin depender de la captura real**
(mock del contrato `AIPipeline`). Deben ser **WAV mono 16 kHz**.

- `sample-en.wav` — "Hello, my name is Isaac. I am learning English every day."
- `sample-en-testing.wav` — "I am testing a speech recognition model that runs in my browser."

Generados con el spike (`src/ai/spike-s1-t7/`, botón **Descargar WAV**). Mono 16 kHz.
